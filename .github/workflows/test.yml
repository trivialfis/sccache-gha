name: Test sccache Action

on: [push]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest, macos-14]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup sccache
        uses: ./.github/actions/sccache
        with:
          version: 'v0.13.0-rapids.0'
          cache-key-prefix: 'sccache-test-${{ matrix.os }}'

      - name: Configure CMake (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cmake -B build -S test -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache

      - name: Configure CMake (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cmake -B build -S test \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache

      - name: Build
        run: cmake --build build --config Release

      - name: Run tests
        shell: bash
        run: ./build/hello_world

      - name: Show sccache stats (mid-build)
        run: sccache --show-stats

      # Build again to verify cache hits
      - name: Clean build directory
        shell: bash
        run: rm -rf build

      - name: Configure CMake second build (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cmake -B build -S test -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache

      - name: Configure CMake second build (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cmake -B build -S test \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache

      - name: Build (should have cache hits)
        run: cmake --build build --config Release

      - name: Show sccache stats (post-build)
        run: sccache --show-stats
